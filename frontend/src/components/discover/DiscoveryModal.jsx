/**
 * DiscoveryModal — legacy shim.
 *
 * All event detail modal behaviour now lives in EventDetailModal.
 * This file re-exports it so any remaining import paths continue to work.
 */
export { default } from "../event-detail/EventDetailModal";
